- the Rust codes create the "foo" file. which contains "verificationKey"(only for test) and "proof".

- use hardhat to read the "foo" file and pass the data to contracts to test.

- the contracts can verify the "proof".



